<?if(!defined('CATALOG_INCLUDED')) die(); ?>
<?

unset($arrFilter);

global $arrTFilter;
$arrTFilter = array(
    "!PROPERTY_VIEW_NEW_1" => false
);


global $arrNFilter;
$arrNFilter = array(
    "!PROPERTY_NEWPRODUCT" => false
);

?>